language = "C"
include_version = true
include_guard = "QRMI_H"
style = "type"
cpp_compat = true
documentation_style = "doxy"
usize_is_size_t = true
pragma_once = true
tab_width = 4
autogen_warning = "/* Warning, this file is autogenerated by cbindgen. Don't modify this manually. */"

after_includes = """

#define QRMI_VERSION_MAJOR 0
#define QRMI_VERSION_MINOR 24
#define QRMI_VERSION_PATCH 0

#define QRMI_VERSION_NUMERIC(M,m,p) ((M)<<16|(m)<<8|(p))
#define QRMI_VERSION (QRMI_VERSION_MAJOR<<16 | QRMI_VERSION_MINOR<<8 | QRMI_VERSION_PATCH)

/**
 *  @defgroup Qrmi Common
 *
 *  Commonly used when using the QRMI C API
 *
 *  @defgroup QrmiConfig QRMI Configuration
 *
 *  Functions used when accessing configuration file data from the QRMI C API.
 *
 *  @defgroup QrmiQuantumResource Quantum Resource
 *
 *  Functions used when accessing Quantum resource from the QRMI C API.
 *
 *  @defgroup QrmiResourceMetadata Quantum Resource Metadata
 *
 *  Functions used when accessing Quantum resource metadata from the QRMI C API.
 *
 *  @defgroup QrmiResourceProvider Resource Provider
 *
 *  Functions used to dynamically discover quantum resources from a vendor
 *  endpoint via the QRMI C API.
 *
 *  @defgroup QrmiService QRMI Service
 *
 *  Functions used to discover the QPU resources assigned to the current job
 *  from the QRMI C API.
 */
"""

[enum]
prefix_with_name = true
rename_variants = "ScreamingSnakeCase"

[export.rename]
"ResourceType" = "QrmiResourceType"
"TaskStatus" = "QrmiTaskStatus"
"Config" = "QrmiConfig"
"QuantumResource" = "QrmiQuantumResource"
"QuantumResources" = "QrmiQuantumResources"
"ResourceProvider" = "QrmiResourceProvider"
"ResourceMetadata" = "QrmiResourceMetadata"
"KeyValue" = "QrmiKeyValue"
"EnvironmentVariables" = "QrmiEnvironmentVariables"
"ResourceDef" = "QrmiResourceDef"
"Payload" = "QrmiPayload"
"ReturnCode" = "QrmiReturnCode"

[export]
# VERSION_INFO is a build-time marker embedded into the `.version_info` ELF
# section for offline inspection via `strings`/`readelf` (see lib.rs). It is
# not a real part of the public C API and isn't meant to be called or read
# from C code, so we exclude it from the generated header. Without this,
# cbindgen emits `extern const uint8_t VERSION_INFO[VERSION_LEN];`, but
# VERSION_LEN is a private (non-`pub`) const that cbindgen silently skips,
# leaving VERSION_LEN undeclared in the generated qrmi.h and breaking the C
# build with "'VERSION_LEN' undeclared here".
exclude = ["VERSION_INFO"]
