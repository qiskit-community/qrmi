// SPDX-License-Identifier: Apache-2.0
// (C) Copyright IBM Corporation 2026
// Generated by: https://openapi-generator.tech

/*
 * IQM Server API
 *
 * > [!CAUTION] >  Please note that this IQM Server API is currently in development and may be subject to changes. For reference, our legacy API documentation can be found [here](?api=integration). However, we advise against integrating with the legacy version, as it will be deprecated soon. Additionally, access to the legacy integration API now requires IP whitelisting by IQM Resonance.   The **IQM Server API** is an HTTP interface that provides programmatic access to IQM Quantum Computer resources. It is used by the official IQM client tools, but external parties can also use it for custom integrations.  The IQM Server API replaces the following legacy APIs:  * Station API (On-Prem only) * Resonance API (Cloud only) * Cocos API (On-Prem and Cloud)  ## API Versioning  The API follows this versioning scheme:  ```plain /api/<version>/<resource> ```  The latest API version can be accessed using the alias `latest`:  ```plain /api/latest/<resource> ```  Available versions can be queried with:  ```plain GET /api/versions ```  #### Major changes (breaking)  When a breaking change is introduced, a new API version is published, and the old one remains available for existing clients. Clients are strongly encouraged to pin to a fixed version for stability. The `latest` alias should only be used for development and testing.  #### Minor changes (non-breaking)  IQM Server may introduce backwards-compatible changes to existing versions. Clients are expected to handle these gracefully.  Data Sent by the Server  * JSON objects: new properties may be added to existing schemas. Clients must ignore unknown properties   (implicit `\"additionalProperties\": true`). * Protobuf messages: new fields may be added. Clients must ignore unknown fields, as described in the   [Protobuf language guide](https://protobuf.dev/programming-guides/proto3/#updating). * Schema narrowing: for example, if a schema was `error_code: int | string`, it may later be restricted   to `error_code: int` without affecting runtime behavior. * Enums and `one_of` fields: variants may be removed as long as runtime behavior is preserved.  Data Sent by the Client  * JSON objects: Clients must not send extra properties. Although the server may ignore them, new optional   input properties added in the future could conflict and cause requests to fail. * Protobuf messages: Clients must not send unknown fields for the same reason.  #### Endpoints  The list of endpoints is not guaranteed to be static. New endpoints may be added to existing versions.  ### Deprecation  When a new version is released, older versions are automatically marked as deprecated. If a client uses a deprecated version, the server will include this response header:  ``` IQM-API-Deprecated: true ```  Deprecated versions may be removed after a deprecation period, so clients should migrate promptly.  The server may also mark individual operations as deprecated. In this case, responses will include:  ``` IQM-API-Operation-Deprecated: true; breaking=<version> ```  Where `<version>` is the API version in which the operation will stop working. If this header is absent, upgrading to a newer version should not break the operation.  ## Authentication  All requests to the IQM Server API require an API Token. The token must be passed as a Bearer token in the `Authorization` header:  ``` curl -H \"Authorization: Bearer <token>\" https://<server-url>/api/<version>/<resource> ```  API tokens are available from the server web dashboard under the profile menu. They are personal and scoped to a single workspace. If a user has multiple workspaces, each requires its own token.
 *
 * The version of the OpenAPI document: v1
 * Contact: support@iqm.tech
 * Generated by: https://openapi-generator.tech
 */

use super::{configuration, ContentType, Error};
use crate::{apis::ResponseContent, models};
use reqwest;
use serde::{de::Error as _, Deserialize, Serialize};

/// struct for typed errors of method [`cancel_job_v1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CancelJobV1Error {
    Status400(models::InvalidInput),
    Status401(models::Unauthorized),
    Status403(models::IllegalJobStatus),
    Status404(models::JobNotFound),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_job_v1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteJobV1Error {
    Status400(models::InvalidInput),
    Status401(models::Unauthorized),
    Status404(models::JobNotFound),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_job_payload_v1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetJobPayloadV1Error {
    Status400(models::InvalidInput),
    Status401(models::Unauthorized),
    Status404(models::JobNotFound),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_job_v1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetJobV1Error {
    Status400(models::InvalidInput),
    Status401(models::Unauthorized),
    Status404(models::JobNotFound),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`job_get_artifacts`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum JobGetArtifactsError {
    Status400(models::InvalidInput),
    Status401(models::Unauthorized),
    Status404(models::JobGetArtifacts404Response),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`job_submit`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum JobSubmitError {
    Status400(models::JobSubmit400Response),
    Status401(models::Unauthorized),
    Status403(models::JobSubmit403Response),
    Status404(models::QcNotFound),
    Status413(models::RequestPayloadTooLarge),
    Status429(models::RateLimitExceeded),
    Status500(models::InternalError),
    UnknownValue(serde_json::Value),
}

///  Cancels a job identified by its job ID.  If the job is currently executing, the system interrupts its execution. Only artifacts produced before the interruption remain available for interrupted jobs. Jobs that have already reached a terminal status cannot be cancelled.
pub async fn cancel_job_v1(
    configuration: &configuration::Configuration,
    job_id: &str,
) -> Result<models::IqmServerJob, Error<CancelJobV1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_job_id = job_id;

    let uri_str = format!(
        "{}/api/v1/jobs/{job_id}/cancel",
        configuration.base_path,
        job_id = crate::apis::urlencode(p_path_job_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::IqmServerJob`"))),
            ContentType::Unsupported(unknown_type) => Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::IqmServerJob`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<CancelJobV1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

pub async fn delete_job_v1(
    configuration: &configuration::Configuration,
    job_id: &str,
) -> Result<(), Error<DeleteJobV1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_job_id = job_id;

    let uri_str = format!(
        "{}/api/v1/jobs/{job_id}",
        configuration.base_path,
        job_id = crate::apis::urlencode(p_path_job_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteJobV1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

///  Retrieves the original input payload of a job identified by its job ID.  The payload is returned exactly as it was at the time of submission, without any modifications. If the job type’s payload schema changes in the future, those changes are not applied to previously submitted job payloads.  The type of the submitted job is provided in the `IQM-Job-Type` response header.  Payloads are stored in a compressed format. For best performance, include `Accept-Encoding: gzip, deflate` in the request headers so that IQM Server can serve the stored data directly.
pub async fn get_job_payload_v1(
    configuration: &configuration::Configuration,
    job_id: &str,
) -> Result<bytes::Bytes, Error<GetJobPayloadV1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_job_id = job_id;

    let uri_str = format!(
        "{}/api/v1/jobs/{job_id}/payload",
        configuration.base_path,
        job_id = crate::apis::urlencode(p_path_job_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        {
            let bytes = resp.bytes().await?;
            Ok(bytes)
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetJobPayloadV1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

///  Retrieves the job status and state metadata of a job identified by its job ID.  Note that this response does not include raw job artifact data (e.g., measurement results). To access artifact data, use the **Get job artifact** endpoint once the artifacts become available for the job.  When `wait_until_terminal=true` is supplied the server holds the request open for up to `timeout_seconds` seconds (default 10, max 30) while it waits for the job to reach a terminal API status (one of `completed`, `failed`, or `cancelled`). If the timeout elapses first, the server responds with the current job state and sets the `IQM-Long-Poll-Timed-Out: true` response header so clients can reissue the request without parsing the body.
pub async fn get_job_v1(
    configuration: &configuration::Configuration,
    job_id: &str,
    wait_until_terminal: Option<bool>,
    timeout_seconds: Option<i32>,
) -> Result<models::IqmServerJob, Error<GetJobV1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_job_id = job_id;
    let p_query_wait_until_terminal = wait_until_terminal;
    let p_query_timeout_seconds = timeout_seconds;

    let uri_str = format!(
        "{}/api/v1/jobs/{job_id}",
        configuration.base_path,
        job_id = crate::apis::urlencode(p_path_job_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_wait_until_terminal {
        req_builder = req_builder.query(&[("wait_until_terminal", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_timeout_seconds {
        req_builder = req_builder.query(&[("timeout_seconds", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::IqmServerJob`"))),
            ContentType::Unsupported(unknown_type) => Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::IqmServerJob`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetJobV1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

///  Retrieves the raw job output artifact data for a job identified by its job ID and artifact type.  The available job artifacts depend on the job type and the Station Control version of the quantum computer. Refer to the auto-generated, quantum computer–specific API documentation for details on supported artifacts and their corresponding data formats.  The list of available artifacts for each job is included in the **Get Job** endpoint response.  Artifacts are stored in a compressed format. For best performance, include `Accept-Encoding: gzip, deflate` in the request headers so that IQM Server can serve the stored data directly.
pub async fn job_get_artifacts(
    configuration: &configuration::Configuration,
    job_id: &str,
    artifact_type: &str,
) -> Result<bytes::Bytes, Error<JobGetArtifactsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_job_id = job_id;
    let p_path_artifact_type = artifact_type;

    let uri_str = format!(
        "{}/api/v1/jobs/{job_id}/artifacts/{artifact_type}",
        configuration.base_path,
        job_id = crate::apis::urlencode(p_path_job_id),
        artifact_type = crate::apis::urlencode(p_path_artifact_type)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        {
            let bytes = resp.bytes().await?;
            Ok(bytes)
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<JobGetArtifactsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

///  Submits a new job for execution. The required payload structure depends on the job type being submitted. Refer to the auto-generated, quantum computer–specific API documentation for details on supported job types and their corresponding payload formats.
pub async fn job_submit(
    configuration: &configuration::Configuration,
    qc: &str,
    job_type: &str,
    use_timeslot: Option<bool>,
    tag: Option<&str>,
    body: Option<serde_json::Value>,
) -> Result<models::IqmServerJob, Error<JobSubmitError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_qc = qc;
    let p_path_job_type = job_type;
    let p_query_use_timeslot = use_timeslot;
    let p_query_tag = tag;
    let p_body_body = body;

    let uri_str = format!(
        "{}/api/v1/jobs/{qc}/{job_type}",
        configuration.base_path,
        qc = p_path_qc,
        job_type = crate::apis::urlencode(p_path_job_type)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref param_value) = p_query_use_timeslot {
        req_builder = req_builder.query(&[("use_timeslot", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_tag {
        req_builder = req_builder.query(&[("tag", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.clone());
    }
    req_builder = req_builder.json(&p_body_body);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::IqmServerJob`"))),
            ContentType::Unsupported(unknown_type) => Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::IqmServerJob`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<JobSubmitError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}
