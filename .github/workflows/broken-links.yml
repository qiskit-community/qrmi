name: Check broken links

on:
  workflow_call:

jobs:

  lychee-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # It will use the lychee.toml as a config file
      - name: Find broken hyperlinks with lychee
        uses: lycheeverse/lychee-action@v2.8.0